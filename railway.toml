[build]
command = "cd backend && npm ci"

[deploy]
startCommand = "cd backend && node src/server.mjs"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
NODE_ENV = "production"
